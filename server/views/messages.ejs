<!-- To display server messages from req.flash-->
<div id="messages">
<% Object.keys(messages).forEach(function (type) { %>
  <div class="alert alert-<%= type %>">
  <% messages[type].forEach(function (message) { %>
    <%= message %>
  <% }) %>
  </div>
<% }) %>
</div>